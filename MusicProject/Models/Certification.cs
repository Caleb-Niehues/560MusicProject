namespace MusicProject.Models
{
    /// <summary>
    /// an enum that represents an album's commercial certification
    /// </summary>
    public enum Certification //this will need reverted to a class or expanded depending on later work
    {
        None = 0,
        Gold,
        Platinum,
        MultiPlatinum,
        Diamond
    }
}
